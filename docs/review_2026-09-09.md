# PolyTheta review — September 9, 2026

## Fixed and implemented

- Exchange sessions, observed holidays, early closes and DST now drive week IDs, expiry dates, schedules, email checks and expiry modeling. The September 7 Labor Day week was missing in the database; Monday-only scheduling could not recover on Tuesday.
- Current views require the actual current week. Old published baskets no longer masquerade as this week's basket or fall back to demo data when the live database is present.
- Data imports validate source dates, current option expiry and cohort freshness. Failed/missing Yahoo records retry; TradingView macro inputs come directly from FRED/Cboe. The weekly universe refreshes from Cboe. Numeric macro defaults no longer hide outages.
- Modeled market refresh prioritizes current-week positions, rotates bounded batches and aborts slow reads before the scheduled function's timeout. Price failures do not suppress independent news checks. This is separate from the IB worker's live portfolio monitoring.
- Basket imports are atomic and preserve existing positions, snapshots and audit history. Delayed email delivery retains its original price/source timestamps and cannot cross into another week.
- Allocation is a configurable equity percentage split equally across whole trades. Call/put percentages determine counts, with configurable maximum trades. Default calls100%, puts0%; no doubling. TSLA and SPCX/SpaceX are excluded by default and editable.
- Per-ticker/side/expiry minimum OTM settings apply in strike selection and at live entry. Delta/ATR rules remain in force. Changing a minimum never edits an existing holding.
- TWS and Web API adapters share deterministic execution rules. They resolve exact contracts and price increments, require real-time execution quotes, request margin previews, preserve intent before submission, reconcile partial fills, and avoid blind write retries.
- Actual holdings, fill costs and IB marks are displayed separately from modeled results. Only attributable PolyTheta trades contribute to P/L and owner-confirmed Exit NOW / Exit all NOW controls. Account fingerprints bind confirmations to the account shown. Exit all pauses entries and includes fills received after the displayed snapshot.
- GSRS is unchanged and visible at `/trading-rules`, including all normalizations, clipping, weights and rounding. Basket GSRS is an entry snapshot, explaining part of its apparent stability.
- Next.js was updated to16.3.4 and compatible vulnerable dependencies were patched. The production dependency audit reports zero vulnerabilities. Four moderate development-tool advisories remain in the Drizzle/tsx esbuild dependency graph; npm's proposed forced fix downgrades Drizzle across breaking versions and was not applied.

## Verification

Independent reviews covered broker safety, data/recovery behavior, authorization and client contracts. All 56 regression tests pass, exercising holiday/DST boundaries, stale data, source recovery, atomic imports, affordable count allocation, minimum OTM, news relevance, broker callbacks/price increments, partial fills, incomplete account refreshes, changed allocation and exit request idempotence. No test contacts a live broker or sends email.

Web typecheck/build and iOS/Watch/macOS builds passed during review. Preview endpoints returned200 for the rules and authenticated settings/status, and401 for unauthenticated broker APIs. The current live connection status correctly reports unconfigured/stale; there is no fabricated account snapshot.

Production release: commit82c5360 passed GitHub verification and deployed to https://polytheta.com (Netlify deploy6aa22c887c0ca1cec6c6b072). Production rules/document routes returned200, unauthenticated broker APIs returned401, and authenticated settings/portfolio reads returned200. Settings retain paused entries, TWS selection and the TSLA/SPCX exclusions. The portfolio reports no broker snapshot and no exit requests, consistent with the unconfigured connection.

The iOS1.5(9) archive, including its Watch companion, exported and uploaded successfully to Apple. Apple reports VALID and IN_BETA_TESTING, and build9 belongs to the existing Andrew internal group. Installation on the owner’s devices remains unverified. The release script now parses dotenv values as data and uses Apple's tool path to avoid the incompatible rsync export failure. The weekly/revalidation LaunchAgents were backed up, updated and reloaded on this Mac; closed-session checks performed no publication or messaging. The IB execution worker was not installed or activated.

## Boundaries and remaining verification

- The local read-only check could not authenticate: `IBKR_ACCOUNT_ID` is not configured, and the standard live Gateway/TWS ports had no listener. Owner execution-host selection, gateway authentication, market-data entitlements and contract/margin read checks remain outstanding. No live orders or activation occurred.
- Pre-existing manual PolyTheta trades need broker-evidence reconciliation before adoption. The code does not infer ownership from a ticker match or adopt all account positions.
- Expiry/assignment without a closing execution requires authoritative IB activity-statement reconciliation. The code flags missing settlement evidence; it does not invent a zero-cost close or act on unattributed stock assignments. Historical Flex statement import is not yet automated.
- News coverage is limited to the configured feed and recognized trusted sources. No code can establish that every acquisition rumor was seen. Apify was not required for the verified data paths and was not subscribed to or activated.
- Legacy modeled history retains prior sizing assumptions. At least one July13 source proposal was generated on July16; this is a historical timing concern, not evidence of achievable entry prices. No historical performance records were silently rewritten.
- The reviewed exchange calendar covers2025–2028. Review new exchange announcements and extend the verified horizon before2029.

See [trading rules](trading_rules.md) and [IB operating guide](ib_operations.md) for the owner-facing controls and configuration.

## September 10 follow-up: approved timing and per-ticker loss stop

- Friday entry uses the final five minutes of the applicable session, including the preceding session when Friday is closed and the actual early close. Monday mode uses the first session of the week. Preparation and final refresh run ahead of the selected entry window. Published baskets retain their original pricing evidence during delivery recovery.
- Entry estimates expose separate calendar-time decay, underlying movement and IV adjustments. An exact option IV is preferred; the configurable VIX approximation is labeled. Current executable quotes are not decayed twice, and actual fills remain separate from modeled returns.
- The configurable maximum loss defaults to 20% **per ticker**, measured against total account NetLiquidation immediately before that ticker's entry. The baseline stays frozen through overlapping exposure and partial fills. The worker compares actual opening/closing cash flows and known fees with fresh IB ask prices for the remaining PolyTheta contracts. A triggered stop persists across restarts, exits only that ticker's attributable holdings, and blocks re-entry into the stopped basket. No broker stop order is placed at entry. Losses across tickers can exceed 20% combined; gaps, unavailable quotes or disconnections can also cause losses beyond a threshold.
- Website, iOS and Watch show the ticker's stop status and actual PolyTheta exposure. The briefing no longer mixes unrelated brokerage holdings or account-wide trading P/L into PolyTheta performance.
- Computer selection now fences every broker write and publication against the selected host and settings revision. The canonical execution journal is stored in the database. Real database verification found timestamp serialization truncating revision microseconds; the fence now preserves the exact database revision and has an installed-driver regression test.

Verification: all 127 tests pass, with no live broker orders or test emails. Web typecheck and build pass; lint has no errors and 21 existing warnings. iOS/Watch simulator, macOS and distribution archive builds pass. Apple accepted iOS1.6(10), build `e06fc34e-054e-4ae4-acad-e584f5f54a0e`, as VALID and IN_BETA_TESTING in the existing Andrew internal group. Device installation remains unverified.

The reviewed web preview is `6aa238c72d35142c44cddc5a`. Rules and document routes and authenticated mobile settings/portfolio reads return200. This Mac is registered and selected as the execution host. The installed execution worker runs every30 seconds and the basket scheduler every60 seconds. The actual database worker-lock check passes; connection status correctly reports that the local IB account ID is not configured. Execution remains disabled and entries paused. Trader Workstation is open for the owner's live corporate login; authentication, socket/API configuration, entitlements and live account/contract reads remain outstanding. The stop is implemented but is not yet protecting a connected live account.

The configured TWS restart window is diagnostic and does not set TWS's own restart preference. Monday orders currently use DAY validity: the worker cancels them after its configured entry window, but cannot guarantee that deadline while disconnected. Settlement/assignment reconciliation limitations from the original review still apply.

Production follow-up: commit `35e64cc0dae4c9ff6e59f943b3d4bb2e0263d4be` passed GitHub Actions run `34440039140`. Netlify deploy `6aa23c0bebddef42f358c3ff` is ready and was published at `2026-09-10T05:12:30.234Z`. Live rules/document routes returned200 and contained the new timing and per-ticker stop rules. Unauthenticated mobile settings/portfolio requests returned401; authenticated reads returned200 and confirmed the20% default, selected Mac, Monday-morning entry mode, paused entries, disabled activation and an honestly stale/missing IB snapshot. Both installed schedules have executed: the broker reports the missing local account configuration and the basket job exits successfully outside its preparation window.
